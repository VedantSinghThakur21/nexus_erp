export const dynamic = 'force-dynamic'

/**
 * Tenant payment detail page (vfixit.avariq.in/payments/[id])
 * Re-exports the main payment detail page.
 */
export { default } from '@/app/(main)/payments/[id]/page'
