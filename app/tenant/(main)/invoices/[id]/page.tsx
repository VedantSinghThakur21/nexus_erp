export const dynamic = 'force-dynamic'

/**
 * Tenant invoice detail page (vfixit.avariq.in/invoices/[id])
 * Re-exports the main invoice detail page.
 */
export { default } from '@/app/(main)/invoices/[id]/page'
